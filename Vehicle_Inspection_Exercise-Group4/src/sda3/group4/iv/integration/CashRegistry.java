package sda3.group4.iv.integration;

import java.util.List;

/**
 * Contains the information of cash flow.
 */
public class CashRegistry {


    /**
     * Handles cash flow.
     * @param amount The amount received.
     */
     public void updateCashRegistry(int amount){

     }

     /**
      * The total cost of the inspection to be paid.
      * @param inspectionList The list where the cost can be calculated.
      * @return The amount to be paid.
      */
     public int calculateCost(List inspectionList){

         return 0;

     }

     /**
      * The change to be
      * @param price TBD this could be a field variable
      * @param amount TBD this could be a field variable
      * @return The amount of change the customer shall have. 
      */
     public int calculateChange(int price, int amount){
         return 0;

     }
}
